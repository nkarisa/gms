#--------------------------------------------------------------------
# Example Environment Configuration file
#
# This file can be used as a starting point for your own
# custom .env files, and contains most of the possible settings
# available in a default install.
#
# By default, all of the settings are commented out. If you want
# to override the setting, you must un-comment it by removing the '#'
# at the beginning of the line.
#--------------------------------------------------------------------

#--------------------------------------------------------------------
# ENVIRONMENT
#--------------------------------------------------------------------

CI_ENVIRONMENT = ${CI_ENVIRONMENT}
LOGTAIL_TOKEN = ${LOGTAIL_TOKEN}
REDIS_SERVER = ${REDIS_SERVER}
REDIS_PORT = ${REDIS_PORT} 
DOCKER_COMPOSE_USAGE = ${DOCKER_COMPOSE_USAGE} 
SESSION_DYNAMODB_TABLE = ${SESSION_DYNAMODB_TABLE}

# New Relic Variable
NEW_RELIC_LICENSE_KEY = ${NEW_RELIC_LICENSE_KEY}
NEW_RELIC_APP_NAME = ${NEW_RELIC_APP_NAME}
NEW_RELIC_LOG_LEVEL = ${NEW_RELIC_LOG_LEVEL}

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------

app.baseURL = ${BASE_URL}
# If you have trouble with `.`, you could also use `_`.
# app_baseURL = ''
# app.forceGlobalSecureRequests = false
# app.CSPEnabled = false

#--------------------------------------------------------------------
# DATABASE
#--------------------------------------------------------------------

database.default.hostname = ${DB_HOST}
database.default.database = ${DB_NAME}
database.default.username = ${DB_USER}
database.default.password = ${DB_PASS}
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306

database.read.hostname = ${DB_HOST}
database.read.database = ${DB_NAME}
database.read.username = ${DB_USER}
database.read.password = ${DB_PASS}
database.read.DBDriver = MySQLi
database.read.DBPrefix = 
database.read.port = 3306

database.write.hostname = ${DB_HOST}
database.write.database = ${DB_NAME}
database.write.username = ${DB_USER}
database.write.password = ${DB_PASS}
database.write.DBDriver = MySQLi
database.write.DBPrefix =
database.write.port = 3306

# If you use MySQLi as tests, first update the values of Config\Database::$tests.
database.tests.hostname = ${DB_HOST}
database.tests.database = ${DB_NAME}
database.tests.username = ${DB_USER}
database.tests.password = ${DB_PASS}
database.tests.DBDriver = MySQLi
database.tests.DBPrefix =
database.tests.charset = utf8mb4
database.tests.DBCollat = utf8mb4_general_ci
database.tests.port = 3306

#--------------------------------------------------------------------
# ENCRYPTION
#--------------------------------------------------------------------

# encryption.key =

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------

# session.driver = 'CodeIgniter\Session\Handlers\FileHandler'
# session.savePath = null

#--------------------------------------------------------------------
# LOGGER
#--------------------------------------------------------------------

# logger.threshold = 4

#----------------------------------------------------------------
# Password salting 
#--------------------------------------------------------------------

SHA256-PASSWORD-SALT = ${SHA256_PASSWORD_SALT}

#----------------------------------------------------------------
# AWS CREDENTIALS 
#--------------------------------------------------------------------

aws.credentials_profile = ${AWS_CREDENTIALS_PROFILE}
aws.region = ${AWS_REGION}

