listen 8080 default_server;
listen [::]:8080 default_server;

# root /var/www/html/public;
root /var/www/devint/public;


# Set allowed "index" files
index index.html index.htm index.php;

server_name _;

charset utf-8;

# Set max upload to 2048M
client_max_body_size 2048M;

# Healthchecks: Set /healthcheck to be the healthcheck URL
location /healthcheck {
    access_log off;

    # set max 5 seconds for healthcheck
    fastcgi_read_timeout 5s;

    include        fastcgi_params;
    fastcgi_param  SCRIPT_NAME     /healthcheck;
    fastcgi_param  SCRIPT_FILENAME /healthcheck;
    fastcgi_pass   127.0.0.1:9000;
}


    # location /devint {
    #     alias /var/www/html/public/; # This tells Nginx to serve requests for /devint from /var/www/html/public

    #     # Explicitly set the index file for this location.
    #     # This tells Nginx to look for index.php (or index.html) when a directory is requested.
    #     index index.php index.html; # Add index.html if you also serve static HTML files

    #     # CodeIgniter's typical rewrite rule for pretty URLs
    #     # This ensures that if a file or directory doesn't exist,
    #     # the request is passed to index.php for CodeIgniter's routing.
    #     try_files $uri $uri/ /devint/index.php?$query_string;
        

    #     # Pass PHP requests to PHP-FPM
    #     location ~ \.php$ {
    #         # This needs to be adjusted based on your PHP-FPM setup.
    #         # Common options:
    #         # fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # Adjust for your PHP version
    #         fastcgi_pass 127.0.0.1:9000; # If PHP-FPM is on a different port/IP

    #         fastcgi_index index.php;
    #         include fastcgi_params;
    #         fastcgi_param SCRIPT_FILENAME $request_filename; # Important for alias
    #          fastcgi_param  CI_ENVIRONMENT development; # Default environment for direct access
    #     }
    # }


# Have NGINX try searching for PHP files as well (This is your original general location block)
# This will act as the default if no /prod/, /devint/, or /stage/ prefix is present.
location / {
    try_files $uri $uri/ /index.php?$query_string;

    # Pass "*.php" files to PHP-FPM (This is your original PHP-FPM block, now nested)
    # The CI_ENVIRONMENT here would be the default if no prefix is matched.
    location ~ \.php$ {
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        fastcgi_param  PATH_INFO $fastcgi_path_info; # Keep this for CI to get path segments
        fastcgi_param  CI_ENVIRONMENT production; # Default environment for direct access
        include        fastcgi_params;
        fastcgi_buffers 8 8k;
        fastcgi_buffer_size 8k;
        fastcgi_read_timeout 99;
    }
}

# additional config
include /etc/nginx/server-opts.d/*.conf;